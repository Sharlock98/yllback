<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yllback.dao.UserDao">
    <select id="queryInfo" resultType="com.example.yllback.entity.UserInfo">
        SELECT * FROM userInfo
    </select>
    <select id="queryInfoById" resultType="com.example.yllback.entity.UserInfo">
        SELECT * FROM userInfo WHERE id=#{id}
    </select>
    <insert id="insertInfo" useGeneratedKeys="true" keyProperty="id"
            keyColumn="id" parameterType="com.example.yllback.entity.UserInfo">
        INSERT INTO
        userInfo(username,password,address)
        VALUES(#{username},#{password},#{address})
    </insert>
    <update id="updateInfo" parameterType="com.example.yllback.entity.UserInfo">
        UPDATE userInfo
        <set>
            <if test="username!=null">username=#{username}</if>
            <if test="password!=null">password=#{password}</if>
            <if test="address!=null">address=#{address}</if>
            WHERE id=#{id}
        </set>
    </update>
    <delete id="deleteInfo">
        DELETE FROM userInfo WHERE id=#{id}
    </delete>
</mapper>